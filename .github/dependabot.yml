# Dependabot: 依赖升级 PR，不占 CI 时间
# 仅对本 repo 生效；其他 repo 可复制到各自 .github/dependabot.yml
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
